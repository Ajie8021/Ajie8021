#### 测试报告

##### 1. 测试概述

###### 1.1 测试目的

本阶段主要对论文查重系统当前实现进行单元测试，验证各核心模块的基本功能是否符合设计预期，并检查测试代码、JUnit 5 与 Maven 测试环境是否能够正常运行。

###### 1.2 测试环境

| 项目 | 内容 |
|---|---|
| 项目 | Java Plagiarism Checker |
| Maven 坐标 | `com.Ajie8021:repo-temp.3224004120` |
| 构建类型 | `jar`25 |
| 测试框架 | JUnit 5 |
| 测试执行工具 | Maven Surefire |
| 测试命令 | `mvn test` |

##### 2. 测试范围

当前项目共包含 4 个测试类，分别覆盖以下模块：

| 测试类 | 测试模块 | 测试数量 |
|---|---|---:|
| `DefaultResultFormatterTest` | 结果格式化 | 4 |
| `DefaultFileServiceTest` | 文件读写 | 3 |
| `DefaultTextPreprocessorTest` | 文本预处理 | 6 |
| `LcsSimilarityCalculatorTest` | 相似度计算 | 10 |
| **合计** | | **23** |

测试覆盖了当前实现中的主要基础模块，包括：

- 文件读取与写入；
- 文本预处理；
- 相似度计算；
- 相似度结果格式化。

其中 `LcsSimilarityCalculatorTest` 针对当前采用的 LCS 相似度计算实现进行了单元测试。

##### 3. 测试执行

在项目根目录执行：

```powershell
mvn test
```

Maven 正常完成项目扫描、编译和测试执行，并自动检测到 JUnit Platform：

```text
Using auto detected provider org.apache.maven.surefire.junitplatform.JUnitPlatformProvider
```

测试执行过程正常，没有出现测试编译错误或运行异常。

##### 4. 测试结果

###### 4.1 分模块结果

| 测试类 | 测试数量 | 失败 | 错误 | 跳过 | 结果 |
|---|---:|---:|---:|---:|---|
| `DefaultResultFormatterTest` | 4 | 0 | 0 | 0 | 通过 |
| `DefaultFileServiceTest` | 3 | 0 | 0 | 0 | 通过 |
| `DefaultTextPreprocessorTest` | 6 | 0 | 0 | 0 | 通过 |
| `LcsSimilarityCalculatorTest` | 10 | 0 | 0 | 0 | 通过 |
| **合计** | **23** | **0** | **0** | **0** | **全部通过** |

Maven 最终测试统计：

```text
Tests run: 23, Failures: 0, Errors: 0, Skipped: 0
```

构建结果：

```text
BUILD SUCCESS
```

Maven 本次执行总耗时：

```text
Total time: 2.315 s
```

###### 4.2 测试通过率

- 测试总数：23
- 通过：23
- 失败：0
- 错误：0
- 跳过：0
- 通过率：**100%**

##### 5. 测试结论

本次单元测试共执行 **23 个测试**，全部通过，未发现测试失败或测试运行错误。

测试结果表明：

1. 当前 Maven 测试工程配置能够正常工作；
2. JUnit 5 测试能够被 Maven Surefire 正确发现并执行；
3. 文件服务模块的当前基础功能测试通过；
4. 文本预处理模块的当前基础功能测试通过；
5. 相似度计算模块的当前单元测试全部通过；
6. 结果格式化模块的当前基础功能测试通过。

因此，**当前版本代码通过了已经编写的单元测试，可以进入后续的官方样例验证阶段。**

##### 6. 测试结果的边界

需要明确，本报告记录的是当前阶段的**单元测试结果**，不能据此直接认定整个论文查重系统已经完成最终验收。

以下工作在本次 `mvn test` 中尚未完成：

- 使用题目提供的官方样例进行完整功能验证；
- 将程序输出与题目规定的标准结果逐项比较；
- JaCoCo 等工具生成的代码覆盖率测试；
- 使用 Profiler 对程序进行性能分析；
- 根据性能分析结果进行优化；
- 优化后的回归测试；
- 最终版本的完整测试。

特别需要注意：当前相似度计算采用的 LCS 方案及其重复率计算公式仍属于当前实现方案，是否完全符合题目最终要求，需要通过官方样例进一步验证。

##### 7. 后续测试计划

后续按照项目开发流程继续进行：

```text
单元测试
   ↓
官方样例验证
   ↓
发现并修正算法/预处理问题
   ↓
补充单元测试
   ↓
代码覆盖率测试
   ↓
Profiler 性能分析
   ↓
性能优化
   ↓
回归测试
   ↓
最终测试
```

官方样例验证阶段应重点关注 `add`、`del`、`dis_1`、`dis_10`、`dis_15` 等不同类型的文本修改样例，以确认当前相似度计算方案与题目要求是否一致。

---

**报告状态：单元测试完成，官方样例验证待进行。**
